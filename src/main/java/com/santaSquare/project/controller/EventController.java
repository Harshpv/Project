package com.santaSquare.project.controller;

public class EventController {
    //admin create,cancel event
    //admin start,schedule event
    //admin invites,removes users from event
    //user updates address before event starts
    //user sends,receives messages once event starts
    //user updates, acks the staus of gift once event starts
}
